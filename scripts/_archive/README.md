# Archived operator scripts

One-off helpers moved here during v3.0 workspace cleanup. Prefer **`fix-wp-urls.cjs`** (Node) over the retired Python copies.

| File | Notes |
|------|--------|
| `fix-wp-urls.py` | Superseded by `../fix-wp-urls.cjs` |
| `check-db.py` | Ad-hoc SQLite probe; use `npm run test:migrations` for schema checks |
