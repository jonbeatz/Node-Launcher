'use client'

/**
 * JEDI_MOD_28 — Station navigation entry (alias of `AppSidebar`).
 * Branding / `msc_mscEngineFooterLine` live in `app-sidebar.tsx` so footer, terminal, and drawer stay in sync.
 */
export { AppSidebar as Navigation, AppSidebar } from './app-sidebar'
