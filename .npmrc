# npm v7+ peers are strict; Next 15.0 peer range + React 19 stable resolves cleanly at runtime
# but fails `npm ci` without this. Keep until Next/eslint peer declarations widen.
legacy-peer-deps=true
