@Vader-Project-Engine.md

# 🚀 INITIALIZE SYSTEM: VADER PROJECT ENGINE (VPE)

**Recipient:** Senior Full-Stack Engineer / System Architect (Cursor Agent)
**Objective:** Initialize the VPE development environment by "stitching" v0 design references into the Electron + Next.js architecture while strictly enforcing the Vader Protocol.

---

## 📂 1. CONTEXTUAL MAPPING (GROUNDING)
Refer to the following workspace files before generating ANY code:
- **`README.md`**: Master Execution Plan.
- **`Node-Launcher-PRD.md`**: Core functional requirements (Nuke/Repair/PM2).
- **`Vader-Project-Engine.md`**: The System UI Specification v2.1.
- **`.cursorrules` & `SKILL.md`**: Global development laws and quality gates.
- **`_design_references/v0-Design`**: Source assets to be mapped to the new architecture.

---

## 🏗️ 2. ARCHITECTURE SETUP
Initialize the following directory structure according to the PRD:
1.  **`src/main/`**: Establish the Electron main process with PM2 integration.
2.  **`src/renderer/`**: Initialize the Next.js frontend with Tailwind CSS.
3.  **`src/preload/`**: Set up secure IPC via `contextBridge` (Vader Shield Protocol).
4.  **`scripts/repair/`**: Create placeholders for the AST logic/Vader-fix scripts.

---

## 🎨 3. UI ENFORCEMENT (VADER PROTOCOL)
Apply the following "Studio Dark" design tokens immediately:
- **Background**: `#121212` | **Surface**: `#1c1c1c` | **Accent**: `#e02b20` (Vader Red).
- **HUD Frame**: 1px horizontal lines at extreme top/bottom edges (30% opacity).
- **Hardware Badge**: Must include "9700x Tuned" pill in project cards.
- **Signature**: "Powered by the MSC Media Engine" must be hardcoded in the Footer.

---

## 💻 4. EXECUTION START
**Task 1:** Read the `package.json` in `_design_references/v0-Design` to identify required dependencies.
**Task 2:** Initialize the root `package.json` for the Electron project, ensuring compatibility with Windows 11 25h2.
**Task 3:** Create a boilerplate "Dashboard Project Grid" in the renderer using the specs from `Vader-Project-Engine.md`.

> **Status:** All `.cursor/rules/*.mdc` are active. Proceed with the initialization of the core Electron main process.